# Clear Mirror for Claude Code

This native wrapper packages the canonical Clear Mirror skill for Claude Code. Load it locally during development:

```bash
claude --plugin-dir ./platforms/claude-code/clear-mirror
```

When loaded as a plugin, invoke the skill with `/clear-mirror:clear-mirror` or let Claude Code discover it when the request matches.
