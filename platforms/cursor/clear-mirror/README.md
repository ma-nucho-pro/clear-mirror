# Clear Mirror for Cursor

This native wrapper packages the canonical Clear Mirror skill for Cursor. Install the local plugin through Cursor's Customize/local-plugin workflow, or copy the skill directory into a supported `.cursor/skills/` location.

The reasoning contract is the same as the portable skill. Cursor-specific hooks and agents are intentionally not mandatory: they depend on the user's version, permissions, and configuration.
