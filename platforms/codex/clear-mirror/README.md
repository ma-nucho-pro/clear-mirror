# Clear Mirror for Codex

The Codex wrapper contains the canonical skill under `skills/clear-mirror/`. Copy that directory into the Agent Skills location supported by your Codex installation, either for the workspace or for the user.

The `.codex-plugin/plugin.json` file is metadata for hosts that support the Codex plugin layout. It does not configure a provider, request credentials, or imply that independent delegation is available.
